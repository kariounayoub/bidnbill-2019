<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-4 col-sm-offset-4 form-n">
      <!-- Insert form here -->
      <h3>New Bid</h3>
      <div class="form-new">

      <%= simple_form_for [@auction, @bid] do |f| %>
        <div class="form-inputs">
          <%= f.input :content, label: "Comment" %>
          <%= f.input :price %>
        </div>
        <div class="form-actions">
          <%= f.button :submit %>
        </div>
      <% end %>
        </div>
    </div>
  </div>
</div>

