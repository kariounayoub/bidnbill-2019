//bouton à cacher class="stripe-button-el"

const btntosshow = document.getElementById("btntoshow")

btntoshow.addEventListener("click", function( event ) {
  document.querySelector('.stripe-button-el').click;
    // Affiche le compte courant de clics à l'intérieur de la div cliquée
    ;
  };
